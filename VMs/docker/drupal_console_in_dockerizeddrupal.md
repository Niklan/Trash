# How to use Drupal Console in dockerized drupal.

```
docker exec -it PROJECTNAME_php_1 bash -l
su -l container
cd /apache/data
```

Thats it, here you can used `drupal` command from drupal console.
